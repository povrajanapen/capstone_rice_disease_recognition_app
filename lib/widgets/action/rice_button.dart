import 'package:flutter/material.dart';

class RiceButton extends StatelessWidget {
  const RiceButton({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}